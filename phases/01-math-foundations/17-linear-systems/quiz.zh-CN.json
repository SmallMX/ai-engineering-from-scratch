{
  "questions": [
    {
      "stage": "pre",
      "question": "中文导读：What does it mean geometrically when a system Ax = b has no exact solution?",
      "options": [
        "中文选项：The matrix A has all zero entries",
        "中文选项：The vector b does not lie in the column space of A",
        "中文选项：The system has more unknowns than equations",
        "中文选项：The matrix A is symmetric"
      ],
      "correct": 1,
      "explanation": "中文解析：In the column picture, Ax = b asks: what linear combination of A's columns produces b? If b is not in the column space (span of A's columns), no exact solution exists. This happens when the system is overdetermined (more equations than unknowns)."
    },
    {
      "stage": "pre",
      "question": "中文导读：Why is partial pivoting used in Gaussian elimination?",
      "options": [
        "中文选项：It reduces the time complexity from O(n^3) to O(n^2)",
        "中文选项：It selects the largest available pivot to minimize error amplification from dividing by small numbers",
        "中文选项：It ensures the result is always an integer",
        "中文选项：It eliminates the need for back substitution"
      ],
      "correct": 1,
      "explanation": "中文解析：Without pivoting, dividing by a small pivot amplifies rounding errors. Partial pivoting swaps rows to place the largest absolute value in the pivot position, keeping the multipliers small and the computation numerically stable."
    },
    {
      "stage": "post",
      "question": "中文导读：Why is Cholesky decomposition preferred over LU for solving (X^T X + lambda I) w = X^T y in ridge regression?",
      "options": [
        "中文选项：Cholesky works on any matrix while LU requires square matrices",
        "中文选项：The matrix X^T X + lambda I is symmetric positive definite, so Cholesky is twice as fast as LU and requires half the storage",
        "中文选项：Cholesky gives a more accurate answer than LU",
        "中文选项：LU decomposition cannot handle regularization terms"
      ],
      "correct": 1,
      "explanation": "中文解析：When lambda > 0, X^T X + lambda I is always symmetric positive definite. Cholesky factors A = LL^T in O(n^3/3) operations — roughly half the O(2n^3/3) of LU — and needs only the lower triangle. It exploits the symmetry that LU does not."
    },
    {
      "stage": "post",
      "question": "中文导读：A matrix has condition number kappa = 10^8. You are using float64 (~15 digits of precision). How many digits of the solution can you trust?",
      "options": [
        "中文选项：About 15 digits",
        "中文选项：About 7 digits (15 - log10(10^8) = 15 - 8)",
        "中文选项：About 8 digits",
        "中文选项：Zero digits — the solution is meaningless"
      ],
      "correct": 1,
      "explanation": "中文解析：You lose approximately log10(kappa) digits of precision. With kappa = 10^8, you lose about 8 digits from float64's ~15 digits, leaving about 7 trustworthy digits. If kappa approaches 10^16, the solution becomes meaningless in float64."
    },
    {
      "stage": "post",
      "question": "中文导读：What is the main advantage of LU decomposition over Gaussian elimination when you need to solve Ax = b for many different b vectors?",
      "options": [
        "中文选项：LU decomposition is more numerically stable",
        "中文选项：The O(n^3) factorization is done once; each subsequent solve with a new b costs only O(n^2)",
        "中文选项：LU decomposition works on rectangular matrices",
        "中文选项：LU always produces a unique solution"
      ],
      "correct": 1,
      "explanation": "中文解析：LU factors A = LU once in O(n^3). Then for each new b, you solve Ly = b (forward substitution) and Ux = y (back substitution), each O(n^2). Gaussian elimination would redo the full O(n^3) for every new b."
    }
  ]
}
